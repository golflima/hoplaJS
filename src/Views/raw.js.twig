/*
 * This script was generated by: HoplaJS, v{{ app.hoplaJS_version }}
 * See: <https://github.com/golflima/hoplaJS>
 * HolpaJS is distributed under the terms of GNU AGPL v3.0 licence
 *
 * This script is not hosted by <{{ request.httpHost }}{{ request.basePath }}> but is generated from data sent in URL after:
 *   <{{ request.schemeAndHttpHost }}{{ request.basePath }}/raw/>
 * To edit this script, go to this page: 
 *   <{{ request.schemeAndHttpHost }}{{ request.basePath }}/edit/{{ data }}>
 *
 * This script hash is: {{ script.hash }} (Warning: other scripts may have the same hash)
 * 
 * This script expects the following dependancies to run properly:
{% for dependency in script.dependencies %}
 *   - {{ dependency }}
{% endfor %}
 *
 * This script expects the following portion of HTML to be present inside the <body></body> section:
 * {{ script.htmlBody }}
 * 
 * And finally, here is the script itself:
 */

{{ script.javascript }}
